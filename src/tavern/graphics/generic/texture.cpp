#include "tavern/graphics/generic/texture.h"

namespace tavern::graphics {

//std::unique_ptr<unsigned char, decltype(std::free)*> load_image_data(const char* path, glm::ivec3& dimensions) {
//    return std::unique_ptr<unsigned char, decltype(std::free)*>(stbi_load(path, &dimensions.x, &dimensions.y, &dimensions.z, 0), std::free);
//}

} /* end of namespace tavern::graphics */
